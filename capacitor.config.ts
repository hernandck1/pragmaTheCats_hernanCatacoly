import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pragma.hernancatacoly.thecats',
  appName: 'pragmaTheCats',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
